// Marker file for the pandaworks-ui `_all` meta-install. Safe to delete after install
// — this file has no runtime behavior. The `_all` registry item exists only to fan out
// every other component via shadcn's registryDependencies resolution.
export const PANDAWORKS_UI_INSTALLED = true;
